import Data from './data.json'

    const data = Data;

    function cadastro({}){
        data.push({
            "id": id,
            "img": img,
            "title": title,
            "descricao": descricao,
            "valor": valor
        })
}

export default cadastro;